const passwords = ['123456', 'password', 'admin', 'root', 'dentalfine', '12345', '12345678', 'admin123', 'admin@dentalfine.com']; async function test() { for (let p of passwords) { try { const res = await fetch('http://localhost:8080/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ correo: 'admin@dentalfine.com', contrasena: p }) }); if (res.ok) { console.log('MATCH FOUND: ' + p); return; } console.log(p + ' failed with ' + res.status); } catch (e) { console.log('Error testing ' + p + ': ' + e.message); } } } test();
